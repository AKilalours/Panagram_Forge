fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.6
pillow>=10.2
numpy>=1.26
pyyaml>=6.0
# CPU wheels. The default index pulls the CUDA build, which is several gigabytes and
# useless here.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2
# Pinned below 5 and paired with sentencepiece for the same reason as pyproject.toml: the
# DeBERTa-v3 tokenizer is a SentencePiece model, and transformers 5 changes how it is built.
# Without this pair the Space boots, downloads the checkpoints, and reports the text
# detector unavailable.
transformers>=4.40,<5
sentencepiece>=0.2
# Below 1.0: transformers 4.x requires huggingface-hub<1.0, and an unbounded requirement
# here would let the Space build install 1.x and break the text arms at import time, on a
# machine nobody is watching.
huggingface_hub>=0.23,<1.0
